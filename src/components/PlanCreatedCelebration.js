// Canonical implementation moved to src/motion/SuccessAnimation.js (the
// Nearby Motion System, CLAUDE.md Item 113) -- kept here as a re-export so
// existing import sites don't all need to change at once. New code should
// import SuccessAnimation from '../motion' directly.
export { default } from '../motion/SuccessAnimation';
