// Canonical implementation moved to src/motion/OccasionAnimation.js (the
// Nearby Motion System, CLAUDE.md Item 113) -- kept here as a re-export so
// existing import sites don't all need to change at once. New code should
// import OccasionAnimation from '../motion' directly.
export { default, OCCASION_SELECT_ANIMATIONS } from '../motion/OccasionAnimation';
