// Canonical implementation moved to src/motion/SurpriseRevealAnimation.js
// (the Nearby Motion System, CLAUDE.md Item 113) -- kept here as a re-export
// so existing import sites don't all need to change at once. New code should
// import SurpriseRevealAnimation from '../motion' directly.
export { default, SURPRISE_REVEAL_TOTAL_MS } from '../motion/SurpriseRevealAnimation';
