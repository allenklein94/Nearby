# Nearby — Project Instructions

Nearby is a proximity-based dating/social discovery app (React Native/Expo, Supabase backend).
Supabase project ref: `enmosvippabmuqslzrox`. A Management API access token lives in
`.claude/mcp.json` (gitignored) — used via direct `curl` against
`https://api.supabase.com/v1/projects/enmosvippabmuqslzrox/database/query` for schema
inspection/migration application, since the Supabase MCP server itself has not been reliably
available via ToolSearch in past sessions.

## Read this first — how this file is organized (2026-09-17, trimmed 2026-09-18)

This file once grew to 30k lines / 2.46MB of per-session build logs and stalled sessions on
compaction. The full historical record now lives, unedited, in **`CLAUDE_HISTORY.md`**
(reverse-chronological; grep by date or "Item NNN"). Open it only when you need the reasoning or
verification trail behind an already-shipped feature — not "just in case."

**Standing rule:** keep this file to a few hundred lines. When a plan/phase finishes:
(1) append its full verbose account to the *top* of `CLAUDE_HISTORY.md`, (2) leave only a short
status line under "Active / unfinished work", (3) fold any new locked convention into "Standing
Conventions" as one bullet, not a narrative.

## Active / unfinished work

Nothing in progress. Everything through **Item 148** (business notification mute groups in `send-push`, web dead-button fix; 147 business email-notification fallback in `send-push`, inert until Resend secrets are set; 146 budget in the Plan read layer, `plans.budget_min`; 145 standalone gatherings + business requests in the Plan read layer, `get_plan_id_for_resource`; 144 dating match + friend connection get a Plan, `get_plan_id_for_match`; 143 participants can open group Plan detail via `get_plan_id_for_group_plan`; 142 plan detail screen on the read layer; 141 universal Plan read layer, `get_plan_overview`; 140 skippable onboarding occasions step; 139 Friends/Dating notification split; 138 onboarding notification prefs; 137 animation audit) (2026-09-18) is done and archived in
`CLAUDE_HISTORY.md` (top section, "Full Active / unfinished work archive" — grep by "Item NNN").
Recent arc for orientation: Items 61-111 Occasions/"Plan for Someone" (wizard, group voting,
surprise mode, packages, recall, guest invites); Items 112-126 the Nearby Motion System
(`src/motion/`, motion language, Reduce Motion, tone per context, anticipation, Nearby Pick,
restrained list animation).

**NEXT SESSION STARTING POINT (set 2026-09-19): Experience plans, the four disclosed gaps.** Everything through the Experience -> one Plan work is built, live and pushed (see "Experience -> one Plan" + "Stop lifecycle" below: `plans` type `experience`, `plan_stops`, `create_experience_plan`, `get_plan_stops`, `link_experience_stop_request`, `ExperienceComponentList`, `PlanDetailScreen`). Pick these up in order; each needs a short design confirm with the owner first (they touch sharing/privacy or the business side):
1. **Reorder / remove stops after creation.** Owner-only RPCs on `plan_stops` (renumber `sort_order`, `unique (plan_id, sort_order)` means shift in two steps); removing a stop that already has a request must not orphan it (cancel or keep the child plan -- decide); re-evaluate parent status after a removal (`sync_parent_plan_status_from_children` counts `plan_stops` business stops). UI on PlanDetail "Your night".
2. **Share the experience with a partner/friends (currently creator-only).** Reuse `plan_organizers` / `add_plan_organizer` (already read by `_can_view_plan` via `is_plan_organizer`) rather than a new participant model; decide organizer vs viewer roles. Check `get_plan_stops` / `get_plan_overview` access and that a shared viewer can't create requests for someone else's stop (`link_experience_stop_request` is creator-only on purpose).
3. **One combined date/time across stops.** Today `scheduled_at` is null and each stop's request carries its own date. Needs a deterministic picker (never AI-inferred), then prefill each stop's ask with it and show one schedule on the Plan; decide sequencing rules (dinner before show) without inventing times.
4. **Business-side awareness of being part of an experience.** Minimum-payload rule applies: a business may learn only that a request is one stop of a multi-part night, at most the stop's structured component label, NEVER the other stops, other businesses, or who else is in it. Decide first whether it is useful to the business at all (opportunity card line) before adding any field to `get_business_opportunities`.
Also open from this arc: gathering stops do not create child plans (attendance is the only signal); the `offer_received` stop state was never exercised live (prod has no offers). Blockers on the owner, unchanged: Anthropic API credit (AI edge functions 500; the Tell Nearby extractor check on the Italian-restaurant example is still pending), Resend secrets for the email fallback.

**Weather loading + unknown forecast (2026-09-20, migration `20270121`, live; audit `PRODUCT_AUDIT/WEATHER_RECOMMENDATION_AUDIT_2026-09-20.md`):** `src/services/weatherLoader.js` replaces the fixed 2 s sleep: submit immediately, poll 500 ms x max 6 (3 s cap), stop on a forecast-known row, fresh cache 10 min per 0.1-degree area + local date, stale up to 30 more min returned as `stale` and never used as a signal, concurrent same-area loads deduped, partial rows not cached, no background retry. `get_weather_result` forecast fields are now NULL (unknown) instead of defaulting to favorable; `isWeatherOutdoorBiased` = `outdoor_favorable === true` (unknown or rain/drizzle pop >= 0.3 = not favorable); indoor bias from current conditions still works with an unknown forecast. Home indoor/outdoor weather lists are upcoming-only (the gathering RPC already required `scheduled_at > now()`; added a load- and render-time re-check). **Rule table + card (same day, migration `20270122` adds `is_daylight` from OpenWeather's d/n icon, live):** `src/constants/weatherRelevance.js` (`weatherLabel`, `homeWeatherCard`) is the one place the Home weather card decides: structured labels only (Storms/Snow/Rain expected, Very warm, Cold out, Poor outdoor conditions, Good outdoor window -- the last only in KNOWN daylight with a KNOWN dry forecast); card renders only with a real upcoming classified gathering to point at and never while an intent result/Surprise is showing. **Activity window (migration `20270123`, live: `forecast_blocks` {dt,temp,pop,id}, `sunrise`, `sunset`):** `utils/weatherWindow.js` `gatheringWeatherWindow(weather, scheduled_at)` judges the weather AT each gathering's own start (covering 3 h block; storm/snow code at pop>=0.3, pop>=0.6 rain, >95F, <45F = indoor; dry + comfortable + known daylight = outdoor; medium pop, no covering block, no sun times, unknown forecast = null). Both the Home card (`homeWeatherCard`, lists filtered per gathering, label = most severe) and the ranking nudge (`weatherAdjustment`) use it. NOT migrated (still current-conditions `weatherBias.js`): intent resolver, Discover, business opportunity scoring. `happeningNow` is now honestly "starting within 30 min" (the 2 h lookback was dead code; started gatherings are neither returned nor joinable), Home section reads "Starting Soon Near You". Jest 984.

**Business "Estimated this month" itemized (2026-09-20, migration `20270124`, live):** the reported "$20 with 0 redemptions" was the live `hybrid` contract's fixed $20 monthly fee (+ $1/redemption) shown under a redemption-only caption -- the amount math was right, the presentation hid the base fee. `get_partner_billing_estimate` (math unchanged) now also returns the fee terms, `base_fee_amount`, `redemption_amount`, `capped`; `utils/billingBreakdown.js` renders "0 redemptions this month / Monthly fee $20.00 + 0 billable redemptions x $1.00 = $0.00". Deliberately NOT forced to $0 for hybrid/flat (the fee is contractual). **Settled figures BUILT (same day, no migration):** under the estimate, "Past invoices" lists the owner's last 6 `business_invoices` rows (`getMyInvoices`, owner-SELECT RLS; `utils/invoiceDisplay.js`: "Aug 2026 · 0 redemptions · $20.00 · Draft, not yet sent"; a draft is never called final/paid). The card is three real tiers: actual redemptions (confirmed count), this month's estimate, past invoices. NOT built, no source data: "offer value" and "potential earnings" (brand_offers carries no dollar value; the card is the platform fee the business owes, not earnings; inventing them would break the no-invented-numbers rule). Web export regenerated; Jest 992.

**Onboarding is reversible (2026-09-20, no migration):** audit found the Questions screen had Back only after step 1, and the Location, Notifications and Login screens had none (Login had only "use a different number"), so a mistaken Get Started could trap someone. Now a shared `components/OnboardingTopBar` ("← Back" + persistent "Already have an account? Sign in") sits on Questions (Back steps within the questions, then leaves), Location and Notifications; Login has "← Back" when it can go back. Progress survives going back: the stack keeps earlier screens mounted, Questions also persists its raw selections in `pending_onboarding_questions_draft` (removed at signup in CompleteProfile), Notifications re-reads its saved choices. CompleteProfile already had Back + Sign Out. Guard: `onboardingReversible.test.js`. Jest 999; not device-tested.

**Expired gathering invitations (2026-09-20, migration `20270125`, live, `scripts/live-verify/social-invite-expiry.sql`):** a `social_invites` invite to a gathering whose `scheduled_at` has passed can be viewed but not accepted. `respond_to_social_invite` (same signature) refuses accept ("This invitation has expired...") and records a dismissal as the new status `expired` (CHECK widened), never `declined`. Activity shows "Invitation expired / August 30 • Past" with only Dismiss (`utils/inviteExpiry.js`); Home banner and Activity badge counts skip expired invites (`countActionableSocialInvites`). Community invites unchanged (no date). Verified live rolled back (past accept refused, past dismiss -> expired, future accept -> accepted). **Extended (same day, migration `20270126`, live, `scripts/live-verify/expired-requests-and-occasion-invites.sql`):** the same rule for gathering join requests (`approve_gathering_interest` refuses a past gathering; the requester sees "Request expired / date • Past" -- derived, no new stored status) and occasion group-plan invites (member `respond_to_occasion_group_plan` + guest link: accept refused once `scheduled_date` has ended in every timezone, dismiss records the new participant status `expired`, no "can't make it" push to the host; no date = never expires; screen shows "Invitation expired" + Dismiss). The anonymous guest page (`docs/occasion-invite.html`) also shows "Invitation expired" for past plans (client-side date check; server refuses the accept regardless), and the business web export was regenerated. Not browser-tested. Jest 1002; not device-tested.

**Category-aware request routing + location notice (2026-09-20, migration `20270130`, live; `scripts/live-verify/category-aware-routing.sql`, `categoryAwareRoutingGuard.test.js`):** `_business_request_fanout` used to route EVERY request to the 10 best-ranked businesses in the radius ignoring category (so a coffee request could miss every coffee shop). Now a request whose `category` maps (via `category_tag_groups`; `request_category_group`) to a group reaches only businesses in that group (`business_in_category_group`: declared tag in the group, or the business's major = the group), ranked exact-tag first (`business_served_tags` contains the requested tag), then the old order (occasion, attributes/cuisine, reputation, distance), cap 10. No category / unmapped category = unfiltered as before. Shared by every request creator (solo, gathering, match, community, plan). This supersedes the earlier "fan-out not changed" note under the canonical category mapping. Rule-based, no AI; no party-size limit (24 or 100 people is a normal request). **Root cause of the Coastal Coffee report:** the only active business had no address/lat/lng, and routing is distance-based, so it was unreachable. The dashboard's address banner now says so plainly (`utils/businessLocationNotice.js`; also flags an address that never geocoded) and clears right after saving (`updateBusinessAddress` returns the coordinates). The owner still has to enter the address; nothing was invented. Not from-scratch replayed; web export regenerated; not device-tested.

**Directed gathering request + AI-free quick response (2026-09-20, migration `20270127`, live; `screen-business-content` redeployed; `scripts/live-verify/directed-gathering-request.sql`):** "Request a specific business" on a gathering (`request_business_partnership`) only made a co-host partnership request (Bookings tab, host's name), so the named business never got an opportunity. Now `_route_gathering_to_partner` also creates/reuses a structured `business_requests` row for the gathering (category = interest tag, party size, date, generic fixed text -- never the host's title) and exactly ONE pending `business_request_offers` row for that business, `is_directed = true` (no fan-out). It appears through `get_business_opportunities` (`is_directed` key) in the normal respond -> offer -> accept flow: card "Coffee gathering / N people / date" with "They asked for your business specifically". Immediate push to the owner (`business_opportunity_received`), excluded from the hourly digest. **Title exception (owner decision, migration `20270128`, live):** on a DIRECTED request only, the gathering's own title is shown to that one business (`gatherings.title` in `get_business_opportunities`, null unless `is_directed`); the title also passes `_title_safe_for_business` (migration `20270129`, rule-based, no AI: no URL/email/phone-like digits/profanity list/>120 chars; a failing title is withheld, never blocks the request; `scripts/live-verify/title-safe-for-business.sql`); broadcast/fan-out requests still carry no host free text, and the minimum-payload rule is otherwise unchanged. Partnership approval (co-host) is unchanged and still separate. No AI anywhere in routing. **Business response without Anthropic:** `screen-business-content` `offer_response` skips the classifier when the description is exactly Nearby's own fixed text (standard availability, + the owner's server-computed usual terms, or the no-note alternative time) and there is no title/items/media; anything owner-typed is still classified and still fails closed with no credit (guard `quickOfferNoAiGuard.test.js`). Not verified end-to-end as a real business user (no auth token); the routing was verified in a rolled-back transaction. No pending partnership request existed live to backfill.

**Gathering "Interested" state (2026-09-20, migrations `20270115` + `20270116`, live; `scripts/live-verify/gathering-interested.js` + `gathering-interested-updates.js`):** Interested -> Going. A private "I might go" in its OWN table `gathering_interested` (NOT a status on `gathering_interest`, the join request: an Interested person never appears in attendee lists, capacity, matches or chat). Writes only via `set_gathering_interested` (join's guards: not host, upcoming, not blocked, invite-only/women-only); `join_gathering` clears it by trigger. GatheringDetail: "☆ Interested" link under Join (primary becomes "I'M GOING"); host sees a count only (`get_gathering_interested_count`). **Keep me updated:** `notify_gathering_updated`/`notify_gathering_cancelled` now also push Interested people (same send-push path, same `notify_planning` mute, same title-only payloads; upcoming gatherings only, blocked skipped); meaningful edit = time, title or place (`area`). Email fallback is business-owners-only in `send-push`, so consumers get push only. **Visibility:** inline only, NO tab -- Home "Your Plans" > "Your interest" and the Plans Upcoming list (`getMyInterestedGatherings`). **Declined/Not attending: deliberately not built.** **Business signal BUILT (migration `20270117`, live, `scripts/live-verify/interested-demand-signal.js`):** ONE separate row kind `gathering_interest` in `get_partner_demand_signals` ("N people nearby are interested in {category} gatherings", category + distinct-people count only). Own floor of 5 (below = no row), never merged into request/search counts (no subtraction), upcoming public `everyone` gatherings only, not women-only/community, categories the business serves through the canonical mapping (below), its 3x3 buckets, 14 days, excludes the business's own hosted gatherings (`hosting_partner_id`) and opted-out people. **Consent:** `profiles.share_interest_in_demand` (default true; Settings > Privacy switch, retroactive because computed at read time) + one-time inline disclosure on first Interested ("Your interest may be included in anonymous local demand trends...", `interested_demand_disclosure_ack_at`, never repeats). A business hosting its own gathering sees its own count via the host-only `get_gathering_interested_count` (no identities). Prod cannot show a real row (too few people); verified with the floor lowered to 2 in a rolled-back txn. Web export regenerated.
**Canonical category mapping (migration `20270118`, live; `src/constants/categoryMapping.js`, `categoryMapping.test.js`):** the two taxonomies were already one -- a business's major (`brand_partners.category`) is a `CATEGORY_GROUPS` key and every leaf tag (gatherings `interest_tag`, request `category`, business `subcategory`/`categories`) belongs to exactly one group -- but SQL only compared exact strings, so a major-only `food_drink` business matched nothing. Added `category_tag_groups` (seeded verbatim from `CATEGORY_GROUPS`; a Jest test fails if the seed drifts) and `business_served_tags(partner)`: a business serves its DECLARED leaf tags (subcategory, secondary categories, posting categories); only one that declared none serves its whole major group (food_drink -> Coffee, Foodie, Wine, ...). Declared tags are never widened. No fuzzy/AI matching; unmapped tag = no match. **Applied only to `get_partner_demand_signals`** (Interested row + the request/search rows on the same card, which had the same mismatch). NOT changed, by decision: `_business_request_fanout`, `_match_request_to_availability`, `get_aggregated_demand_for_partner` (Match Radar), `search_active_business_availability` and the client resolver/experience matching -- they have their own category hierarchy and matching them to the mapping would change unrelated behavior. If group-level equivalence is wanted (Coffee shop <-> Wine gathering), change `business_served_tags` only. Verified live rolled-back (`interested-demand-signal.js`). Jest 956. Not device-tested.

**Who can join? (2026-09-20, migration `20270119`, live, `scripts/live-verify/gathering-requires-approval.js`):** hosts choose **Anyone** (default; one tap, auto-approved -- what public gatherings already did) or **Require approval** (`gatherings.requires_approval`; Create "who" step + Edit switch; hidden for invite-only, which already requires review). `join_gathering` = pending when `requires_approval` OR non-public; capacity still waitlists first. **Waitlist promotion respects approval (migration `20270120`, live, `scripts/live-verify/waitlist-promotion-approval.js` + `waitlistPromotionGuard.test.js`):** one helper `_promote_from_waitlist` is used by `leave_gathering` and `host_remove_gathering_attendee`; on an approval-required (or non-public) gathering the earliest waitlisted person becomes PENDING (host approves via the existing flow; host + person get a push from `notify_gathering_approved`'s new waitlisted->pending branch), otherwise automatic approval + match as before. A pending person holds the freed slot for the decision only, so a withdrawal or decline promotes the next waiter and never over-promotes. Client rule in one place: `utils/gatheringJoinMode.js` (`needsApproval`, `joinLabel`; used by Detail/Gatherings/DiscoverHub). Host moderation gap closed: `host_remove_gathering_attendee` (host-only; declines a pending/waitlisted request or removes an approved attendee, promotes the waitlist; a declined requester gets a neutral `gathering_updated` push, a removed attendee none) with Decline/Remove buttons on the Gatherings hosting list. A pending join request is now worded "Requested"/"Request sent" (was "Interested"), so "Interested" only means the private maybe state. Report/block already exist app-wide. Web export regenerated; Jest 961; not device-tested.

**Standing open item (device-only verification):** no simulator/device tooling has ever been
available, so all UI/animation/push work is unit-tested (Jest 600/600) and transform-checked but
never exercised on a real device. First session with device access should spot-check: motion
components with Reduce Motion on/off, pull-to-refresh, the N loader, tab/mode/filter transitions,
push tap routing, calendar export, and view-shot share cards.

Known disclosed gaps (not built, no ask yet): pre-filter of surprise-excluded person in
BusinessRequestDetail invite pickers (server enforces it); Cuisine as a browse filter; gathering-
destined occasion co-organizers; push for `plan_messages`; anon `is_match_participant` grant
error on `business_requests` reads (fails closed).

**State-machine audit (2026-09-19, `PRODUCT_AUDIT/STATE_MACHINE_AUDIT_2026-09-19.md`): gaps 1-4 (migration `20261210_reservation_plan_lifecycle`) 6 (`20261211_all_declined_outcome`) and 5 (`20261212_parent_plan_status_from_children`) FIXED and live: all audit gaps closed.** A cancelled primary reservation ends the request + Plan (+ fulfilled group
plan; the request does not reopen); completing one sets the Plan `completed`; the host can cancel a group plan in any non-terminal
state, cascading downstream and notifying participants; the occasion "Planned" badge follows the Plan's live status. Gap 6 is a derived
outcome (`utils/requestOutcome.js`, no stored status): the decline that leaves no live offer sends one `business_request_all_declined`
push and the detail screen shows a wider-radius banner; a last *withdrawal* shows the banner but sends no push. Gap 5: a trigger recomputes an `occasion` parent plan's status from its child plans (match plans deliberately excluded --
long-lived containers; group_occasion already follows its group plan). Cancellation reason analytics (`20261213_cancellation_events`): every root cancel (reservation, request, gathering, occasion group plan)
is recorded automatically in `cancellation_events` (cascades not double-counted); the reason is OPTIONAL, asked after the cancel via
`CancellationReasonSheet` -> `set_cancellation_reason` (never gates a cancel); the business owner sees aggregates in the dashboard's
"Cancelled Reservations" (`get_partner_cancellation_patterns`), owner-visible insight only, never auto-reweights matching. `cancel_community` and legacy `cancel_group_plan` record too (`20261214`); `stopRecurringSeries` only sets a flag, deliberately not
recorded. Nothing else open from this audit. Not exercised on real data (prod has no reservations).

**Progressive personalization (2026-09-19, requirement: onboarding sets up the engine; explicit > behavior early, blended later).**
The five signal classes already existed as `signalSourceMaturity.js` (explicit/contextual never dampened; behavioral/social/
transactional scaled by account maturity). New: a private behavioral signal (`behavior_events`, `20261215`: open/create/join per
canonical category; owner-only RLS, RPC-only writes, 1h dedupe, 90-day window, Settings "Clear my activity history", never read by
businesses, guarded by `behaviorPrivacyGuard.test.js`) and `src/constants/blendedRanking.js` (explicit 5 pts > behavior max 4,
behavior x maturity). `usePersonalization()` feeds Gatherings Nearby + For You (`rankByBlend`/`forYouBlend`) and Discover
(`behaviorNudge` on `fit.score`). Home already used the maturity model. No "save" feature exists, so saves are not tracked (build the
surface first). Social/transaction signals are unchanged (already in the resolver / positive-experience signals). Not device-tested.

**Onboarding flow reshaped (2026-09-19, requirement: onboarding leads naturally into the product).** Welcome -> "What do you want Nearby
to help you do?" (6 goals, multi-select, `constants/onboardingGoals.js`) -> "What are you into?" (groups, then favorites) -> "What are you
looking for?" (Dating / Friends / Both / Skip) -> relevant preferences (comfort; every step skippable, with Back) -> Location -> "What should
Nearby keep you posted about?" -> account -> "You're ready ... Let's see what's happening nearby ->". Goals and looking-for are stored in the
existing `onboarding_motivations` (looking-for as the tokens 'Go on dates' / 'Make new friends' that People sub-mode and Friend Discovery
already read; no schema change). The celebrations step now shows only for people who picked "Plan celebrations". Home now reflects the goals: a "What you're here to do" shortcut
row above Quick Picks (`goalShortcuts` in `onboardingGoals.js`, `useMyGoals`), one chip per picked goal to an existing screen (Discover people/things,
MakeAPlan, Places, CelebrateSomething, BrandOffers); nothing renders for anyone with no saved goals. Goals are editable in Settings ("What you're here to do", saves per tap via `motivationsWithGoals`, keeps looking-for tokens); Home refetches on focus. Not device-tested.

**Onboarding wiring audit (2026-09-19, `PRODUCT_AUDIT/ONBOARDING_WIRING_MATRIX_2026-09-19.md`):** matrix of every onboarding input vs its consumers. Fixed two orphans (the UI-only location options; comfort level now also ranks the Gatherings feed). Follow-up: picked interest groups are saved as `profiles.interest_groups` (`20261216`) = a weak broad signal (2 pts vs a declared tag's 5), never expanded into tags; editable in Settings; ranks Gatherings/For You/Discover/Home recommendations only.

**"Demand near you" (2026-09-19, first business-signal-loop increment; migration `20261217_partner_demand_signals`, live):** one card at the top of the business dashboard (also in the web export) from `get_partner_demand_signals` -- EXPLICIT demand only (`business_requests` + `intent_submissions`; never behavior/impressions/raw text), a signal returns only when >= 5 DISTINCT people back it (person counted once across requests+searches), owner-only, only the business's own categories, geography = Match Radar's rules (requester radius / 3x3 `wide_area`), 14-day window. Category rows add a party-size bucket and budget range only when each independently clears the floor; occasion rows come from requests. Each row's action reuses an existing flow (category -> Post availability, occasion -> Create package). Below the floor the card says it is still gathering activity. `intent_submissions.party_size` added (written from `classifyResult.partySize`). Verified live with disposable rolled-back data (threshold, owner check, radius, business_partner searches excluded, dedupe). Guards: `demandSignals*.test.js`. **Dashboard-wide floor (migration `20261218_demand_privacy_floor_dashboard_wide`, live):** `demand_min_people()` = 5 is enforced in the data layer for every business-facing demand aggregate -- Match Radar (`get_aggregated_demand_for_partner`), What They're Planning (`get_occasion_demand_for_partner`), and the two business push triggers (`notify_aggregated_demand_threshold`/`notify_occasion_demand_threshold`, previously fired at 2, now at the 5th distinct person). Counts are DISTINCT PEOPLE not request rows; secondary figures (party total, soonest date, dominant period/category, weekend count) are nulled/zeroed when they rest on <5 people; Match Radar's unmet-search count excludes `business_partner` searches. Any new business-facing demand aggregate must use `demand_min_people()`. Not floored on purpose: own-page view counts and own-customer stats (not demand about unconnected people), the friends-only trigger `notify_group_intent_threshold`. The community leader push (`notify_community_area_demand_threshold`) is a coordination trigger, NOT business demand exposure: it stays at 2 (`20261219` floored it; `20261220_community_demand_push_restore` reverted it, live). Rule: the floor of 5 governs business-facing aggregates about unconnected people only; consumer/community pushes stay at 2; a business's own first-party stats are never floored. The card reads "Last 14 days"; budget stays "Budget $X-$Y" (semantics don't establish per-person). Verified live (4 people/repeat requester -> nothing, 5th -> rows + pushes, unmet 4 -> hidden, 5th -> shown) and by a full from-scratch Docker replay (212 migrations clean; 366 public functions = prod, six touched functions hash-identical to prod, single overloads). Not device-tested.

**Full from-scratch Docker replay (2026-09-19):** all 209 migrations (through `20261215_behavior_events`; re-run 2026-09-19 through `20261218`, 212 migrations, still clean) applied cleanly to an empty
`supabase/postgres:15.1.0.147` public schema with `ON_ERROR_STOP`; every function touched this session has exactly one overload, both new
tables/the parent-plan trigger exist, and the public function count matches production (364 = 364).

**Business opportunity card simplified (2026-09-19):** the pending-opportunity card on the business dashboard is now "✨ New opportunity" -> title (occasion · category) -> one who/when line ("6 people · Fri, Sep 25 · 7–8 PM") -> one feel/budget line -> "Customer is looking for" (cuisine/attributes/dietary) -> Send Offer / Can't accommodate. Built by `utils/businessOpportunityCard.js` from structured fields only (no invented "$$$"/"Upscale" tier; the per-reason "🎯 why you match" lines were dropped from the card, scoring/sort unchanged). Web export regenerated. Not device/browser-tested.

**Business side = Nearby matches, business responds (2026-09-19):** matching was already server-side (fanout + scoring/sort); this framed it that way. The Opportunities section now headlines "N new opportunities that fit your business" (real count of open pending), helper says Nearby matched them, the Brief card line reads "N new opportunities fit your business — view them", and the empty state is an invitation to Post availability (no dead end). The flow after View (request card -> offer with availability/details -> accept/decline) already existed and is unchanged. Not built (no ask): a periodic digest push ("3 new opportunities"); pushes stay per-request. Web export regenerated; not browser-tested.

**Business opportunity digest push (2026-09-19, migration `20261227`, live; send-push v14):** hourly cron `send-business-opportunity-digests` -> `send_business_opportunity_digests()` sends one quiet push "N new opportunities that fit your business" (count only, no request content) to each business owner with unseen (`viewed_at` null), still-open, non-urgent matched opportunities created since their last digest; at most one per owner per 6h (`business_opportunity_digest_state`, seeded at migration). **Per-request `business_opportunity_received` pushes now fire only for urgent requests** (`_opportunity_is_urgent`: date today/tomorrow), everything else waits for the digest -- to restore per-request pushes, drop the `continue when not _opportunity_is_urgent(...)` line in `_business_request_fanout`. Type `business_opportunities_digest` is in the "requests" mute group, quiet tier, emailed to web-only owners, and taps route to the dashboard like a single opportunity. No timezone quiet-hours (no per-owner timezone stored; the push is quiet-tier). The email fallback is tested by running the real `send-push` handler with stubbed deps (`src/constants/sendPushDigestEmail.test.js`: verified+enabled address only, mute honored, token owners get the push not the email); reuse that loader to test other send-push behavior. Verified live with rolled-back data (urgent excluded, count-only body, 6h gap). Not device-tested.

**Business preferences (2026-09-19, migration `20261228`, live):** the business "tell Nearby once" model already existed in pieces (offer: attributes/cuisine/categories/accommodates_party_types/group size; want more: `priority_attributes`, `priority_time_windows`, `priority_occasions`, time-bounded priority signals). Added to "want more" timing: **Weekdays** and **Last-minute bookings** (`priority_time_windows` += `weekday`, `last_minute`; matched from the request's own date in `scoreBusinessOpportunity`, last-minute = today/tomorrow, same rule as `_opportunity_is_urgent`); the section is now headed "What do you want more of?". **Not built, by decision:** demographic targeting (discrimination/legal risk, no ask to override); "slow hours" (no per-business hours data -- the morning/afternoon/evening/weekday chips are how a business says which hours to fill); multiple cuisines (`brand_partners.cuisine` is single-valued); "offers birthdays/anniversaries" as a capability distinct from wanting them (occasions are want-more only). Web export regenerated; not browser-tested.

**Attributes `private_dining` + `corporate_events` (2026-09-19, migration `20261229`, live):** added to the ONE attribute vocabulary (now 20 keys) -- a business offers them (profile), can want more of them, a customer can ask for them (AskBusiness Preferences), "Teach Nearby" extracts them from text, and existing attribute-overlap scoring matches them (no new scoring). The migration widens all 6 CHECKs and the 6 validating RPCs in place from their live bodies (guarded to fail loudly). It also **repaired pre-existing drift**: `business_partner_requests`/`business_experiences`/`priority_attributes` + `set_business_priority_attributes`/`create_/update_business_experience` allowed only the original 8, so picking most tags under "want more" would have errored. Edge functions `create-assistant`, `business-onboarding-assistant`, `screen-business-content` redeployed. `attributeVocabulary.test.js` now guards client list == every constraint == every edge-function list. The two are business-only: `VENUE_PREFERENCE_OPTIONS` (the list minus `BUSINESS_ONLY_ATTRIBUTE_KEYS`) feeds every consumer dining-preference surface (onboarding prompt modal, Profile venue prefs, friend "vibe" poll), guarded by `attributeVocabulary.test.js`; business profile, want-more and request Preferences keep the full list. Adding a future attribute = one migration like this + the three edge lists + `BUSINESS_ATTRIBUTE_OPTIONS`. Web export regenerated; not browser-tested.

**Effortless business response (2026-09-19):** the pending opportunity card now asks "Can you accommodate this?" with **Accept / Offer Alternative / Decline**. Accept -> "What's your offer?": **Standard availability** (one tap, sends an `offered` `standard` offer with the fixed line "We can accommodate this as requested.") or **Special offer** (opens the unchanged full offer editor). Offer Alternative -> a short sheet: time picker (opens on the requested date/time, never pre-submitted) + optional note -> `alt_time` offer with `proposed_time`. Decline -> the existing reason picker. No server change: it reuses `submit_business_offer` via `screen-business-content` (which requires a non-empty description, hence the fixed factual lines in `utils/quickOfferResponse.js`, never AI text). **Usual-terms preset:** Standard availability appends the owner's own standing-policy terms (`usualTermsLine`): minimum spend per person, deposit and cancellation window -- the deposit is worded "arranged directly with us" because Nearby does not collect it (no charge fires; the policy editor label says so) -- and never `max_discount_pct` (a ceiling, not an offer). The Accept sheet previews the exact terms that will be sent, or offers "Set your usual terms" (-> the policy editor) when none are set. Verified live (rolled back): both payloads become `offered` offers. Shared result handling is `handleOfferResult`. Not device-tested; web export regenerated.

**Max discount cap enforced server-side (2026-09-19, migration `20261230`, live; `screen-business-content` redeployed):** `business_fulfillment_policies.max_discount_pct` was stored-only and offers had no structured discount. Now offers and availability postings carry `discount_pct`, and ONE trigger on `business_request_offers` (`enforce_offer_discount_cap` -> `_discount_cap_violation`) checks every path that makes an offer `offered`: cap comes from the business's ACTIVE policy only (no policy / paused / null cap = unlimited and no percentage required, the prior behavior); with a cap set, `offer_type='discount'` REQUIRES a percentage and any stated percentage must be <= cap. Manual paths (`submit_business_offer`, screening approval) raise a clear error; auto-created offers (availability/package-sourced) are silently not created so a bad posting never aborts a request fan-out (the request stays pending); availability offers inherit the posting's `discount_pct`; `post_business_availability` also checks at posting time; the edge function pre-checks so nothing over cap is held for review. Policy auto-accept and AI auto-respond only make `standard` no-discount offers, so they pass. Dashboard forms show a Discount % field for the Discount type with the cap and mirror the rule (`utils/discountCap.js`). Known limit: lowering the cap does not retroactively edit existing postings; their future auto-offers just stop. Verified live with cleaned-up data (`scripts/live-verify/max-discount-cap-enforcement.js`: at/below/above cap, missing pct, no policy, paused policy, auto-accept, posting, lowered cap); Jest 818/818; web export regenerated. Not device/browser-tested; no from-scratch Docker replay.

**Supply first: scheduled availability + demand preview (2026-09-19, migration `20261231`, live; `screen-business-content` redeployed):** Post Availability now has "When do you have space?" -- **Now** (the old live-for-N-hours chips) or **Pick a day & time** (explicit start/end pickers, e.g. Friday 6-8 PM; both go through the edge function as `startsAt`/`endsAt`, re-validated, and the screening-approval branch honors them and refuses a window that has passed). While the sheet is open, `get_availability_demand_preview` (owner-only, debounced) mirrors `post_business_availability`'s matching predicates and returns the DISTINCT-PEOPLE count of open matching requests, NULL below `demand_min_people()` (5) -- the sheet then shows "✨ 7 people nearby are looking for dinner Friday." (`utils/availabilityWindow.js`), nothing at all below the floor (never "0"), and the button reads **Send Offer** when a count is shown. Posting itself is unchanged: it already offers to matching requests (latest 10). Like Match Radar, exact counts above the floor are shown, so a determined owner varying windows could learn "at least 5" boundaries; same residual as the other aggregates. Verified live in one rolled-back transaction (`scripts/live-verify/availability-demand-preview.js`: floor, distinct people, non-owner refused, day/category/capacity/time misses, scheduled post offers to matches). Jest 830; web export regenerated. Prod only has 4 profiles, so a real 5-person count was exercised with the floor temporarily lowered to 3 inside the rolled-back transaction. Not device/browser-tested.

**Occasions we offer (2026-09-19, migration `20270101`, live):** a new, explicit business capability, separate from "want more" (`priority_occasions` = appetite/ranking; `brand_partners.offered_occasions` = "we do this"). Six chips on the dashboard (saved per tap, `set_business_offered_occasions`, owner-only, CHECK-guarded): Birthday, Anniversary, Date Night, Celebration, Graduation, **Group/Family** (= the existing `family_gathering` key; no "group events" key exists). No second engine: the package path is unchanged (a request still gets an ACTIVE PACKAGE for its occasion as an offer), and `_business_request_fanout` now ranks a business that offers the request's occasion FIRST, so it is among the 10 businesses reached even with no package -- it then gets an ordinary pending opportunity (no package/offer is invented; verified). `scoreBusinessOpportunity` credits an offered occasion once (weaker weight, never stacked on want-more). The demand line is now "N nearby customers are planning an anniversary" + "M of them this weekend" + "You offer this" (the old copy said "this weekend" for the whole group); counts unchanged from `get_occasion_demand_for_partner`, distinct people, hidden under `demand_min_people()` (5). Consumer-side discovery: see the next paragraph. Verified live in a rolled-back transaction with 30 disposable clone businesses (`scripts/live-verify/occasions-we-offer.js`); Jest 838; web export regenerated. Not device/browser-tested.

**Occasions we offer -- consumer-side discovery (2026-09-19, migration `20270102`, live):** `resolveIntent` now has a fourth business tier, `resolveOccasionOfferingBusinesses` -> `search_occasion_offering_businesses(occasion, lat, lng, radius)` (active, geo-scoped, only when the ask carries a real occasion and location; returns id/name/distance only). It reuses the `business_policy_match` candidate type (marked `viaOccasionOffering`) so Home/Celebrate/Surprise render and route it as the existing honest "may be able to help ... business confirmation required" card -- "X offers Birthday experiences" -- never "available". Its score (`occasionOfferingScore`, max 3) sits below the confirmed-availability/package floor (4) by construction (tested). `dedupeBusinessTiers` now enforces ONE card per business at its strongest tier: live posting > package > offers-this-occasion > policy-only (live slot + package from one business both stay, as before; NOTE a policy-only business that also has a package is now shown once, where it used to be shown twice). `search_active_business_availability` gained a trailing `offered_occasions` column (old function dropped first, single overload) and `occasionBonus`/reasons use `businessFitsOccasion` (offered OR want-more, never stacked). Verified live, rolled back (`scripts/live-verify/consumer-occasion-offering-discovery.js`); Jest 842. Consumer app only -- no business web export change. **Vocabulary audit (same session):** one occasion vocabulary -- `OCCASION_OPTIONS`; the six offerable keys are accepted by every live constraint that matters (`brand_partners` offered + priority, `business_occasion_packages`, `business_requests`), by the consumer intent extractor (`create-assistant` `VALID_OCCASIONS`), and onboarding/celebrate keys are subsets of the one list (guards in `occasionsWeOffer.test.js` + the live-verify script). One deliberate exception: availability *bundles* (`business_availability.bundle_occasion`, a separate feature) accept only five occasions (no graduation) -- not part of "we offer". Privacy: the discovery RPC is authenticated-only and returns business id/name/distance, no people or demand; business-facing demand counts are untouched and still floored at 5. When a business offers the occasion but has no live posting/package the card never claims availability and no offer is created.

**"Why this matches" on business opportunities (2026-09-19):** a pending opportunity card now reads "✨ Good match for your business" + "Why" bullets (`buildMatchReasons` in `utils/businessOpportunityCard.js`, fed by the scoring reasons, which now carry a stable `key`). Lines only for signals that really fired (offers/wants the occasion, attribute/cuisine/party-size/timing/boost) plus "within the area they asked for" (true by construction: fan-out is radius-scoped). With no real signal it stays "✨ New opportunity". Deliberately NOT claimed: price-range fit (budget_max not established per-person) and availability (no per-business hours data). Web export regenerated; Jest 848; not browser-tested.

## Backlog (v2 candidates — not started, do not build without a direct ask)

Deliberately out of scope for "Group planning for an Occasion" (archived in CLAUDE_HISTORY.md) per the user's own
explicit v1 guardrail — the killer loop is Remember → Plan → Invite → Find something → Connect
business → Do it, and these would each turn it into a "giant event-management platform" the user
specifically said not to build yet:

- Complex RSVP systems (beyond the current plain invited/joined/declined on
  `occasion_group_plan_participants`)
- Elaborate invitations (custom invite text/design, themed invite cards, etc.)
- Gift registries
- Seating charts
- Massive event pages (a dedicated rich event-detail surface beyond `GroupOccasionPlanScreen`'s
  current lean voting view)
- Complicated calendars (recurring sub-events, multi-day itineraries, etc. — beyond the single
  `scheduled_date` the occasion already has)
- **Qualitative "High demand" label on the business demand card (added 2026-09-19; not started).** A "High demand this weekend"-style
  tag across rows. Blocked on a real basis: any cutoff for "high" would be an invented number, which the no-invented-numbers rule
  forbids. Build only once there is real comparison data (e.g. this area/category vs its own trailing average, or vs comparable areas),
  and it must respect `demand_min_people()` (5) like every other business-facing aggregate.

- **Business "offer value" / "potential earnings" on the billing card (added 2026-09-20; not started).** The billing card is the platform fee the business owes, not business earnings, and no source data exists (`brand_offers` has no dollar value; redemptions carry no spend). Build only after a product decision on where a real number comes from (e.g. the owner enters a value per offer, or a redemption records spend), then label it plainly as owner-entered, never inferred. Also open from the same arc: weather activity-window is Home-only (intent resolver, Discover and business opportunity scoring still use the current-conditions `weatherBias.js`); Home hero copy "Perfect night..." style AI-flavored weather lines stay out.

**Business dashboard = five tabs (2026-09-19; simplified same day to the owner's four nouns, guarded by `businessDashboardSimplicity.test.js`):** the tab formerly "Offers" is now **Availability** ("Tell us when you have room": posting + your live postings; Occasion Packages, Rewards & Offers and Signature Experiences sit behind a collapsed "More ways to offer"), **Occasions we offer moved to Profile**, and Profile leads with "Tell us what you offer" (Tell Nearby card, business profile, accommodations, want-more) with Fulfillment Policy, Notifications, Stripe, Reservation Provider and AI Automation behind one collapsed "Settings" row. **"Demand near you" moved from Home to Opportunities** (below the opportunity list, then "What They're Planning") so aggregated demand reads as the wider view of the same opportunities; its self-link summary is dropped there since the section headline already carries the count. Tab keys are unchanged (`offers`), so push taps/legacy routes still work. Nothing was deleted or rewritten, only regrouped/gated. Original description: Home / Opportunities / Bookings / Offers / Profile (was six: Dashboard, Gatherings, Community, Opportunities, Insights, Business). Regrouped by gating existing sub-blocks per tab in `BusinessDashboardScreen` (`on()`/`tool()`), nothing rewritten or removed. Home = brief, demand-near-you, community health; Opportunities = opportunity cards + What They're Planning; Bookings = upcoming visits, gatherings, community/top members, returning customers, confirm a redemption; Offers = occasions we offer, availability, packages, rewards, signature experiences; Profile = fulfillment policy, business profile/preferences, notifications, Stripe, AI Automation Settings. **"More tools"** is a collapsed row at the bottom of Home (never a tab): AI Assistant (navigates), Analytics, Weather, Demand Signals (Match Radar) expand in place. Old names (`requests`, `business`, `insights`, ...) still route via `LEGACY_SECTION_TAB` (push taps use `initialSection: 'requests'`). Not browser-tested; web export regenerated.

**Business website == app parity (2026-09-19, requirement: an owner can do receive -> review -> offer -> accept -> manage -> decline -> update availability -> manage reservations from either surface).** Both run the same `BusinessDashboardScreen`, so parity lives in the platform shell. Audit found ONE real break: react-native-web's `Alert.alert` is a no-op, so on the website every confirmation (Cancel Reservation, delete/pause package, remove experience), validation message and error silently vanished -- Cancel Reservation could not run at all. Fixed by `src/utils/webAlert.js` (browser alert/confirm adapter, installed in `App.web.js`; native untouched). Date/time inputs were already covered (`PlatformDateTimeInput`); redemption confirmation works via the 6-digit code entry (only the camera scanner is native). `businessWebParityGuard.test.js` guards the seams. **Follow-up (same day): the four disclosed gaps were closed** -- see the next paragraph. Not browser-tested; web export regenerated; Jest 854.

**Business web parity, round 2 (2026-09-19; tracker `PRODUCT_AUDIT/BUSINESS_WEB_PARITY_PROGRESS.md`).** Owners no longer get "open the app" for anything on the site (guard test forbids the string and requires every dashboard `navigate` target to be registered in `BusinessWebNavigator`).
(1) **Gatherings/communities on web:** the real consumer screens (create/edit/detail/chat, ViewProfile, GatheringConfirmation) are registered in the web navigator, unmodified; `SelectGatheringLocationScreen.web.js` replaces the map picker (business address / address search / browser location); `App.web.js` wraps a DISABLED PostHog client because the shared screens call `usePostHog()`. Not registered: consumer-only screens the detail screens can link to (AskBusiness, RequestBusinessPartner, BusinessRequestDetail, PlanDetail, Paywall) -- a tap there is a no-op on web.
(2) **"QR scanner" was a false premise:** the app has NO QR scanner (no expo-camera). What was hidden on web was "Post a Moment" camera capture; it now works on web via the browser picker (`captureStoryMedia`/`uploadStory` web branches). Redemption confirmation is 6-digit code entry on both.
(3) **Stripe on web, TEST MODE ONLY until the owner approves live.** `stripeMode()` labels test mode; onboarding passes `platform:'web'` and the function returns to `BUSINESS_WEB_URL` (server-decided, default the GitHub Pages business URL; never client-supplied). **Hard gate in both Stripe-key functions (`business-stripe-connect-onboarding`, `create-business-payment-intent`, deployed):** a live key (`sk_live_`/`rk_live_`) returns 503 unless the secret `STRIPE_LIVE_APPROVED=true` is set BY THE OWNER; no code sets it. Approval checkpoints (all are stops, "build it/continue" is never approval): (1) creating/connecting a live connected account, (2) live business/KYC submission, (3) accepting Stripe terms, (4) linking a bank/debit card, (5) enabling live payouts, (6) first live money movement. KYC, terms and bank details happen only inside Stripe-hosted onboarding; Nearby never collects or stores them. No Stripe secrets exist in prod today, so nothing live is possible. NOT built (suggested, no ask): an approval audit log / per-checkpoint approver roles.
(4) **Email fallback:** already built (Item 147); now logs a warning and returns `emailReason` from `send-push` when an owner's verified alert can't be sent (never silent). **Blocked on the owner:** Resend account + verified domain + `RESEND_API_KEY`, `EMAIL_FROM` (+ optional `BUSINESS_WEB_URL`) secrets. Emails go only to owners with no push token.
Not browser-tested (no browser available); web export regenerated; Jest 856.

**Stripe approval AUTHORITY (locked 2026-09-19; extends the six checkpoints above).** Who may approve, per checkpoint. Claude/engineering are NEVER financial approvers: they build, configure test mode, run tests, validate webhooks, inspect non-sensitive status and prepare the exact action; they never self-approve, impersonate an approver, accept Stripe terms, submit KYC attestations, add/change payout destinations, enable live payouts or start live-money movement. "Finish Stripe / deploy this / go live" is NOT approval of any checkpoint.
- **Business-controlled, NO Nearby fallback (1 enter live onboarding, 2 business/KYC submission, 3 Stripe terms, 4 external payout account):** only the business owner or a legally authorized representative, through the Stripe-hosted flow. Nearby admins, support, developers and Claude may not enter, attest, accept or substitute. If they are unavailable the step stays pending until another legally authorized person for that business completes it. Nearby never requests, stores or transmits identity/bank credentials.
- **Checkpoint 5 (enable live payouts):** business owner/authorized rep authorizes for their business; Nearby may expose the control and show Stripe status but never silently enables payouts, and no approver may bypass incomplete Stripe requirements. Nearby-side operational approval: platform owner/admin, fallback a separately designated Nearby finance/operations approver.
- **Checkpoint 6 (first live-money movement):** the business authorizes its own customer transaction via the normal offer/reservation flow; the Nearby platform owner/admin approves activating a live-money mechanism (fallback: the designated finance/ops approver). A NEW payment-routing/transfer/payout/Stripe architecture needs the platform owner (the fallback only if explicitly granted that authority).
- **Fallback approver must be** a named person, independently authenticated, holding an explicit financial-approval permission, different from the implementer when practical, and approving the specific action. A fallback can never accept business terms, submit KYC, attest ownership, link/change a bank account, impersonate the owner, override Stripe requirements, or approve a transaction the business itself has not authorized. No authorized approver available -> FAIL CLOSED (stay pending).
- **Every live checkpoint must record** who approved -> what -> when -> which business/Stripe account -> resulting Stripe state; a fallback approval also records primary-unavailable, fallback identity, reason and decision. No generic "admin approved" flag.
- **Not built yet (no live step exists to record):** the approval audit table and approver permissions. Build them before the first live step, not after. **Caveat:** the `STRIPE_LIVE_APPROVED` secret is a coarse deploy-time kill-switch that keeps a live key inert; it is NOT an approval record and setting it does not satisfy any checkpoint above.

**Consumer never feels a marketplace (locked 2026-09-19; companion to Item 136).** The consumer arc is: *I want something -> Nearby: we found some options -> the business: here's what I can do -> I'll take this one -> Nearby: you're booked.* Never: search a marketplace -> contact a business -> negotiate -> wait -> sort out the reservation -> leave the app. Apply as a tiebreaker to any consumer flow touching businesses: Nearby does the finding and matching, the business responds with a concrete offer, acceptance IS the booking, and the confirmation is Nearby's voice ("You're booked"), with no step that sends the consumer off-app to finish. The request flow (AskBusiness -> BusinessRequestDetail offers -> accept -> reservation) already follows it; 2026-09-19 copy pass: confirmation reads "You're booked" (was "Reservation confirmed"/"Accepted -- your reservation") and the payment-unavailable notice no longer tells the consumer to "reach out to them directly". **Marketplace residue closed (2026-09-19):** Brand Offers no longer has a per-offer Message button; the business profile's primary action is "✨ Plan something" (-> AskBusiness prefilled with the business's category), and "💬 Your conversation" appears only when a thread already exists (a business may have replied), so starting a chat is no longer offered. The offers list is headed "✨ Nearby found N options for you" ("Here's what each can do"), the accept button reads "I'll take this one", and "✨ Our pick" marks the first offer only when it leads on an established reliability record (same rule that orders the list; never invented). Ordering logic unchanged. Jest 860; not device-tested.

**Master requirement: signals -> business opportunities, and the feedback loop (locked 2026-09-19).** Every relevant consumer signal (onboarding preferences, interests, intent, searches, plans, occasions, group size, timing, location context, budget, availability needs, relevant behavior) maps to an appropriate business-side signal WITHOUT exposing unnecessary personal information: businesses get the minimum needed (need -> relevant context -> opportunity -> offer), as extremely simple opportunity cards with a clear reason for the match and one-tap Accept / Send Offer / Offer Alternative / Decline; consumers control what is shared where appropriate; web and app share ONE state machine with equivalent core function; unfulfilled demand and successful/unsuccessful outcomes are captured so matching keeps improving. **Standard: sophisticated underneath, ridiculously simple on top** -- the business surface stays far simpler than the intelligence behind it. Existing coverage is in the Standing Conventions (minimum payloads, floor of 5, progressive personalization); the two increments below close named gaps.
- **Experience assembly: context-triggered "Make it a night" + two new recipes (2026-09-19; no migration).** The framework already existed (Sep 10: `EXPERIENCE_TEMPLATES`, `assembleExperience`, business bundles); it only fired for an explicit extracted occasion. Now, with no occasion template, `experienceContextKey` (deterministic, from signals the extractor already returns) maps a planning-window ask (`dateWindow` today/tonight/tomorrow/weekend; never now/flexible/null) to a `CONTEXT_TEMPLATES` recipe: `partyType` date -> date night ("✨ Make it a night"), `kid_friendly` -> family day, `partyType` friends -> day out (Something to Do / Food / Drinks). A context experience is a SUGGESTION: needs real inventory in >= 2 components, no bundles, never claims items (the flat results stay exactly as they were), and an explicit occasion template always wins unchanged. Category arrays stay canonical (`categoryVocabularyGuard.test.js`). NOT built: places/perks as component inventory (only business postings + gatherings fill components), AI-assisted category selection, `family_day`/`friends_out` occasion keys for Celebrate. Jest 927; not device-tested.
- **"Tell Nearby about your business" (2026-09-19; edge function deploy PENDING the user):** an existing owner types a sentence or two at the top of the dashboard Profile tab (`TellNearbyBusinessCard`), Nearby reads it back as chips ("We understood your business"), and "Yes, continue" saves through the existing setters (`updateBusinessProfile`, `setBusinessOfferedOccasions`, `setBusinessAccommodations`). **Additive only** (`utils/businessSetupPlan.js`): the primary category/subcategory/cuisine are filled only when empty; attributes, secondary categories, occasions we offer and party types only gain entries; "want more" (`priority_occasions`) is deliberately never written from text. `business-onboarding-assistant` now also returns `offeredOccasions` (the six offerable keys) and `partyTypes` (solo/friends/groups/date), validated server-side; guarded by `attributeVocabulary.test.js`. NOT extracted, by decision: price range (no real source; only the owner's own policy terms) and availability patterns (no per-business hours data). **Edit** makes each highlighted (new) chip tappable to leave it out (saved = exactly what stays checked; excluding a category also drops its subcategory/cuisine) and links to the EXISTING Edit Profile modal (`openEditProfileModal`) for category/cuisine/attributes -- no second editor. Saved values are the same fields existing matching already reads (`occasionBonus`/`businessFitsOccasion`, `accommodatesPartyTypeBonus`, `scoreBusinessOpportunity`; tested); setters' owner-only authz verified live rolled-back (`scripts/live-verify/tell-nearby-setters-authz.js`). Needs `supabase functions deploy business-onboarding-assistant`; until deployed the card still works but returns none of the two new fields. Jest 920; web export regenerated; not browser/device-tested.
- **Weekend framing + "Your business matches N" (2026-09-19, migration `20270111`, verified live rolled-back via `scripts/live-verify/demand-weekend-framing.js`; live in prod, confirmed 2026-09-19):** category, group and occasion rows of `get_partner_demand_signals` gain `weekend_count` = distinct people whose REQUEST is dated in the coming Fri-Sun (same definition as `get_occasion_demand_for_partner`); requests only (searches have no date), floored at 5 on its own and withheld when 0 < people_count - weekend_count < 5; card copy "N of them this weekend"; absent means absent. The card also opens with "Your business matches N open requests right now -> View opportunities", the owner's OWN open pending opportunities (first-party, not floored). Deliberately NOT built: a qualitative "High demand" label (would need an invented threshold). Jest 904; web export regenerated; not browser-tested.
- **Post-availability count floored (2026-09-19, migration `20270109`, live, `scripts/live-verify/post-availability-count-floor.js`):** `post_business_availability` returned the raw number of requests it offered to -- a demand figure about unconnected people that bypassed the preview's floor. `matchedCount` is now returned only when >= `demand_min_people()` DISTINCT requesters are behind it, else null (offers/pushes unchanged); the confirmation then reads "It's live. Nearby sends it to matching requests nearby..." with no number and no View-opportunities link. NOTE the general rule this exposes: an individual request routed to a matching business is NOT floored (it must reach a business to be fulfilled; minimum payload, no identity) -- the floor governs AGGREGATE demand figures only. Jest 898; web export regenerated.
- **Fill a Gap polish + Groups row (2026-09-19, migration `20270108`, live, `scripts/live-verify/demand-group-row.js`):** "Demand near you" gains a "Groups of 6+ are looking nearby" row (distinct people, requests + searches, party_size >= 6 across the categories the business offers; same floor of 5, absent below it; count only). Category rows show "You have N open opportunities in this category" + a View opportunities link, counted client-side from the owner's OWN pending open opportunities (first-party, so deliberately not floored). The Post Availability confirmation ("Nearby found N matching requests") gains a "View opportunities ->" link to the Opportunities tab when N > 0. Not built: a weekend framing across rows. Jest 896; web export regenerated; not browser-tested.
- **One-tap smart offer + Large groups (2026-09-19, migration `20270107`, live):** "Special offer" -> Customize now opens the editor already started from the owner's OWN active package that fits the request (same occasion, party clears `min_guests`: title, price per person, included items; banner says which package) with a "For this request" header (occasion/category, party/date/time, budget line); with no matching package it opens as before, nothing filled, and Nearby never writes a price. Nothing is sent until Send; all fields editable. Not built: a fallback from the owner's last offer for that occasion (needs a server read; no source data on the opportunity rows), and paused packages are not used. "What do you want more of?" gains **Large groups** (`priority_time_windows` += `large_group`, same CHECK+setter widening as weekday/last_minute; matched client-side from the request's `party_size >= 7` = the demand card's 7+ bucket; card reason "A large group, which you want more of"). Verified live (rolled back): setter accepts `large_group`, rejects junk. Jest 894; web export regenerated; not browser-tested.
- **Demand when/outdoor (migration `20270106`, live, `scripts/live-verify/demand-when-and-outdoor.js`):** category rows also carry `when_day`+`when_period` (most-requested weekday x morning/afternoon/evening cell, from `business_requests.date` + `time_window_start`; a request missing either contributes nothing, no time is ever inferred) and `outdoor` (structured `outdoor_seating` attribute), each floored at 5 distinct people AND checked against the row's visible people count (a fact is withheld if 0 < people_count - cell < 5, so subtracting shown numbers can't expose a hidden group under 5). Absent means absent, never 0/partial. Independent facts, not a claim that the same people did both; requests only (searches carry no date/attributes); party bucket is still category-wide, not joint with them. Card copy: "Friday evening · Outdoor seating · N people". Jest 890; no from-scratch Docker replay; web export regenerated.
- **Unfulfilled demand (migration `20270103`, live):** category rows of `get_partner_demand_signals` add `unfulfilled_count` (distinct people whose still-open request has no offer yet) and `supply_count` (active businesses within 15 mi offering the category). Unfulfilled has its own floor of 5 (else null, and supply is withheld with it); requests only (searches have no fulfilled state); card copy "N still waiting for an offer · M businesses offer this nearby". Verified live, rolled back, floor lowered to 3 inside the transaction (`scripts/live-verify/unfulfilled-demand-signal.js`). **Not built:** the time-of-week/period ("Friday evenings") and free-form "outdoor group dining" dimensions from the example (structured fields only: category, party bucket, budget); an occasion-row unfulfilled count.
- **Feedback loop (migration `20270104`, live):** the existing post-plan outcome (`OfferOutcomeModal`: experience + would-repeat) gains an optional **"Good match?"** (`business_offer_outcomes.match_fit` yes/somewhat/no, private, consumer-only). `get_my_positive_experience_signals` now also returns `category_names` (kinds the person loved: gathering interest tags + business request categories, only when they would do it again and the match was not "no"); Home credits "You loved this kind of experience last time" once, never stacked on a repeat host, TRANSACTIONAL weight (maturity-dampened). Gathering `interest_tag` and business `category` match only where they are the same string. **Not built:** `match_fit` is now aggregated (migration `20270112`, live, `scripts/live-verify/partner-match-fit-aggregate.js`): owner-only `get_partner_match_fit`, a single line "How well your matches land: X% yes · Y% somewhat · Z% no" under Offer Performance (Bookings/insights area, no new tab), returned only when >= `demand_min_people()` (5) DISTINCT people answered, percentages only, no identities; `get_partner_offer_reputation` (consumer-facing) is unchanged; the ⭐ star scale (kept the existing four-face scale, per the older no-second-scale decision). Jest 860; no from-scratch Docker replay; not device-tested.

**Experience -> one Plan (2026-09-19, migration `20270113`, live, `scripts/live-verify/experience-plans.js`):** in a "Make it a night"/occasion Experience each item with real supply (availability posting or gathering; never perks/packages/communities) has "+ Add to your night" (one pick per component, `ExperienceComponentList` on Home intent results, Discover intent search and Celebrate free-text search; NOT Celebrate's own options list or Surprise Me); with >= 2 parts picked "Plan this night · N stops" calls `create_experience_plan` -> a `plans` row of type `experience` + ordered `plan_stops` (2-6, distinct components; titles/partner resolved SERVER-side from a still-active posting of an active business with capacity, or an existing gathering; no date invented, `scheduled_at` null, nothing booked). PlanDetail shows "Your night" with a Continue per stop that opens the existing flow (business -> AskBusiness bound to that posting via `matchedAvailability`; gathering -> GatheringDetail); Plans lists it as "Night out". `get_plan_stops` is view-checked via `_can_view_plan`. **Stop lifecycle (migration `20270114`, live, `scripts/live-verify/experience-stop-lifecycle.js`):** AskBusiness started from a stop passes `experienceStopId`; on submit `link_experience_stop_request` (owner-only) binds the request to the stop and re-parents the request's own Plan as a CHILD of the experience Plan (best-effort; never blocks the request). `get_plan_stops` derives each stop's state (chosen / requested / offer_received / booked / done / cancelled; gathering stop = booked when the caller is an approved attendee) and PlanDetail shows it, with "View ->" to the request once asked (a cancelled stop can be asked again). Parent status reuses `sync_parent_plan_status_from_children`: an experience stays `draft` until EVERY business stop has a request and every live one is confirmed, then `confirmed`; completes/cancels only when no stop is live. Request Plans that are experience stops are hidden from the standalone list. Verified live rolled-back, except the `offer_received` state (no offers exist in prod to read). **Not built (disclosed):** no reordering/removing stops after creation, creator-only (no sharing with a partner), no combined date/time across stops, no business-side notion of being part of an experience, gathering stops don't create child plans. Not from-scratch replayed; Jest 946; not device-tested.

**Docs for review (2026-09-19):** `PRODUCT_AUDIT/FIRST_DEVICE_SESSION_CHECKLIST_2026-09-19.md` (ordered device/browser smoke test, automated vs real-device split) and `PRODUCT_AUDIT/EXPERIENCE_PLACES_PERKS_PROPOSAL_2026-09-19.md` (Places/Perks as Experience components). **Perks BUILT as an add-on line only** (`attachPerk` in `experienceAssembly.js`, `ExperiencePerkLine`): a targeted perk whose `target_interest_tag` exactly equals a category of the SAME business's availability item shows as "+ Perk: ..." under it; no score, never changes order, never fills/counts toward a component or an experience, flat results unchanged (Jest 934). **Places NOT built** (unconfirmable Google results; would break the marketplace-free promise); an "Also nearby (not a Nearby partner)" row is still an open owner decision. Not device-tested. **Blocker found 2026-09-19:** the project's `ANTHROPIC_API_KEY` returns "credit balance is too low", so every AI edge function 500s; the Tell Nearby extractor end-to-end check (Italian-restaurant example) could not be run and is still open until credit is added.

Deferred from "Why this matches" (2026-09-19, business opportunity cards; no ask yet):
- **Budget contract (locked 2026-09-19):** `business_requests.budget_max` is the maximum spend PER PERSON in whole dollars; `party_size` is separate; nothing migrated (consumer chips/inputs, group-plan `agreed_budget_max`, scoring, demand percentiles were already per person). `src/utils/budgetTier.js` is the one implementation: tier `$` (0 < x <= 25) / `$$` (<= 75) / `$$$` (> 75), computed from the per-person number only (never party total, never AI); null = no budget/no tier; zero/negative invalid (client `resolveBudgetMax` never yields them; migration `20270105`, live, tightens the CHECK to `budget_max > 0`, NOT VALID so no row is touched). Business card reads "Up to $60/person · $360 for the party · $$" (total = budget x party_size, only when both are real); demand card says "/person"; "Their budget fits your price range" fires only when budget_max >= the owner's ACTIVE `min_spend_per_person` (both per person, party size never involved). The consumer $/$$/$$$ chips (25/60/150) map to their own tier under the same cutoffs (tested). Jest 886; web export regenerated; not browser-tested.
- **"You have availability"** -- BUILT (2026-09-19) as "You have space posted for that time": only when the owner's own ACTIVE `business_availability` posting covers the request (same category or uncategorized; window contains the requested time, else overlaps the day; `availabilityCoversRequest`). No standing hours exist, so no claim otherwise. Jest 864; web export regenerated; not browser-tested.

**Pending user action (not a build item):** connect Resend so the business email fallback (Item 147/148) actually sends.
Needs the user: verify a sending domain in Resend, create a Sending-access API key, then set Edge secrets `RESEND_API_KEY`,
`EMAIL_FROM` (verified domain), optional `BUSINESS_WEB_URL` via `supabase secrets set --project-ref enmosvippabmuqslzrox`
(user runs it themselves; never paste the key in chat). As of 2026-09-19 none are set; the feature is inert and the dashboard
card says so. Verify by listing secret NAMES only via the Management API `/secrets`, then the user adds an address in the
dashboard card and confirms the 6-digit code (the only real end-to-end test).

## Standing Conventions (Locked)

These are the load-bearing rules distilled from thousands of lines of prior build history. Full
original reasoning/citations for any of these: `CLAUDE_HISTORY.md`.

- **Businesses don't manage Nearby; Nearby manages the complexity for businesses (locked 2026-09-19, owner directive).** Underneath: classification, intent resolution, demand aggregation, matching, availability, offer generation, reservation state, notifications, analytics, preferences, occasion/behavior signals. On top the owner sees only four things: **Opportunities** (something you can fulfill), **Bookings** (what's confirmed), **Availability** ("tell us when you have room"), **Profile** ("tell us what you offer"). Three signal layers exist and must stay distinct in the data layer but merge in the owner's eyes into "N opportunities for you": (1) individual demand = one request routed to a matching business, structured and identity-free (never "Allen wants..."; see the minimum-payload rule), (2) aggregated demand = "Demand near you", floored at 5 people, (3) business opportunity = the owner's own matched requests. The loop: consumer says what they want -> Nearby interprets -> picks appropriate businesses -> simple opportunity -> business responds -> consumer chooses -> reservation -> outcome (match-fit, cancellation reasons, decline reasons) feeds back. Tiebreaker for any business-side change: if it adds a concept the owner must learn, push it into Nearby instead. Do not add tabs or settings screens for it.
- **Business-facing payloads carry the minimum needed (2026-09-19, locked).** A business receives only what it needs to evaluate, offer on and
  fulfill that specific request: structured fields (occasion, category, party size, budget, date/time, closed-vocabulary `attributes`/`cuisine`) plus
  `summary`, a line built ONLY from those by `business_safe_request_summary()` (migration `20261221`). Never consumer free text (`raw_text`,
  `plan_label`), profile-derived data (`shared_interests`), or internal ids (`match_id`/`gathering_id`/`requester_id`), in an RPC payload OR a push
  body to a business. Consumer-side systems keep and use those fields internally. The consumer "share interests" toggle and free-text "Anything else?"
  note were removed from AskBusiness because they no longer reach businesses. `attributes` is DB-CHECK-constrained. Dietary needs are a separate closed vocabulary (`business_requests.dietary`, `DIETARY_OPTIONS`, migration `20261223`): consumer-picked per Foodie request in AskBusiness, never AI-inferred, shown to a business only via `get_business_opportunities` for a request it has an offer on, deliberately NOT in `summary` or any push body. Carried by solo, gathering, match and community requests (`20261225`, shared validator `normalize_dietary`); one shared `DietaryPicker` component: AskBusiness (any Foodie ask) and DateProposalScreen (shown to the accepter of a Foodie plan that already has a chosen place, where `handleRespond` auto-creates the request; its "Find Somewhere to Go" button routes to AskBusiness). Group plans (`20261226`): each participant declares their OWN needs on GroupPlanScreen (owner-only table `group_plan_participant_dietary`, `set_my_group_plan_dietary`; never copied from the inviter), and `confirm_group_plan` puts the unattributed UNION of accepted participants' needs on the one request; a dessert add-on inherits its parent's needs, other add-on types get none. NOT carried by `invite_to_business_request` (an invitee's own request). Verified live with rolled-back data (union, add-on inheritance, no cross-user read).
  The Stripe PaymentIntent description (created on the business's connected account, so business-visible) uses the same summary (`create-business-payment-intent` v2, helper granted to service_role in `20261224`); no edge function reads `raw_text`. Guarded by `src/utils/businessPayloadPrivacyGuard.test.js`. Any new business-facing payload must be a structured projection, never `select *`/raw text.
- **No invented numbers, no fabricated signals, ever.** Every metric/count/reason shown anywhere
  in the app must trace to a real query result. An absent signal renders as an honest empty
  state, never a guessed placeholder.
- **The Nearby Motion Language (locked 2026-09-18, `src/constants/motionLanguage.js`).** A fixed
  6-glyph vocabulary for animation/loading/celebration moments: N (NearbyMark) = system
  intelligence (loading/searching/finding/matching/recommending); ✨ = discovery/recommendation
  (a transition beat, not a standalone icon); ❤️ = romantic connection (a dating match); 🤝 =
  platonic connection (a friend match, an organizer relationship — this app's own pre-existing
  handshake motif, kept distinct from ❤️); 🎉 = celebration (occasion/plan/milestone); ✓ =
  completion (plan/reservation/request confirmed); 🔒 = privacy/surprise. Never reach for 🎉 as a
  generic "something good happened" glyph for a connection moment — that's what ❤️/🤝 are for.
- **Animation discipline (locked 2026-09-18).** Animations must reinforce meaning, hierarchy,
  state changes, or feedback — never "because we can." Every animated/celebratory moment must be
  short, subtle, interruptible, never gate or slow down a real button/action, and call
  `src/hooks/useReduceMotion.js`, collapsing to its final/settled visual state (content and
  meaning intact, motion only removed) when it returns true.
  Item 127 (2026-09-18) enforces this at the shared level: `src/motion/motionPolicy.js` holds the one
  Reduce Motion store and, installed at app start (`App.js`), turns every `Animated.spring` into an
  instant timing and every `Animated.loop` into a no-op while it's on; the stack navigator swaps to
  a plain fade. Prefer `Animated.timing` fades for new work; branch on `useReduceMotion()` only where
  the reduced state is different content (skip a morph/particle burst, drop a slide offset).
  Item 128: motion must work in both light and dark -- draw only with `useTheme()` tokens (never a
  hardcoded hex; `#fff` only over a fixed dark overlay/brand-coral surface), and any new token a
  motion piece reads gets a light+dark value guarded by `src/motion/motionThemeContrast.test.js`.
  Item 129: haptics accompany only IMPORTANT state changes, via the named beats in
  `src/motion/haptics.js` (`playHaptic`): match/friendAccepted = subtle light impact, success (plan/
  reservation confirmed, plan created, surprise revealed) = success notification. Never per-tap;
  independent of Reduce Motion; never call expo-haptics directly from a motion component.
  Item 130: haptics are for USER-INITIATED moments only. Motion components take an opt-in
  `haptic` prop (default false); pass it only when the animation is the direct result of the user's
  own tap. Arrival-driven states (realtime update, refocus discovery, a push) get the animation but
  no haptic, and `services/notifications.js` never touches expo-haptics -- a background push keeps
  normal OS notification behavior. Guarded by `src/motion/hapticsRespect.test.js`.
  Item 131: every transition is authored against the motion budget in `src/motion/motionBudget.js` --
  tiny 50-150ms (button/icon), small 150-300 (filters/tabs/cards), medium 300-500 (plan confirm, match),
  special 500-900 (occasion celebration, surprise reveal). Measured trigger -> settled state; holds
  and ambient loading loops are exempt. Sequence timings are `SEQUENCES` tokens shared by the
  components and `motionBudget.test.js`; nothing may exceed 900ms. New sequences add a token + test.
  Items 132/133 (loading language, two meanings): `NLoader` (N + brand sweep) = "Nearby is thinking/
  finding/matching" -- use `<NLoader fullScreen={false} size="compact|inline" kind=... | caption=... />`
  for search, recommendations, availability, discovery (kinds in `loadingLanguage.js`; captions narrate
  real work, never counts/percentages). `SkeletonFeed` (`src/motion/`) = "a known feed's content is
  loading" -- use for large lists of the user's own content (matches, friends, plans, timeline,
  activity, gatherings/people feeds, load-more footers) so the screen feels fast and doesn't jump.
  Plain `ActivityIndicator` stays only for in-flight feedback inside a button/footer control.
  Item 134: an empty state that carries a real next-step action uses `<FadeInState opportunity>` --
  the N appears, then the invitation + action settle in (medium tier). Only with a real tappable
  action (guarded by `emptyInvitation.test.js`); errors and private/admin/chat empties stay plain fades.
  Item 135: intent searches narrate their REAL phases (`runIntentSearch(text, { onPhase })`;
  `intentPhaseCaption()`: Understanding your request… -> Finding activities…/Finding communities…/
  Checking availability…), then `FoundLine` ("Here's what we found.") as results land. Phases are
  reported as the work actually moves, never on a timer -- no artificial delay for theater.
- **Preference wiring (2026-09-18, requirement; plan in `PRODUCT_AUDIT/PREFERENCE_WIRING_AUDIT_2026-09-18.md`).**
  Capture a preference once (onboarding where appropriate, progressive otherwise), store it once, and
  feed every consumer from that one value -- one canonical category/interest vocabulary (`PLACE_CATEGORIES`
  / `CATEGORY_GROUPS`), no per-feature category lists, no captured-but-unused fields.
- **Location is asked once, used everywhere (2026-09-18).** Device position comes only from `src/services/userLocation.js` (`getUserLocation`/`requireUserLocation`): permission checked once, fixes shared/cached, fallback to last-known then stored position; the app never asks the user where they are. Direct expo-location permission/position calls are allow-listed in `locationCentralGuard.test.js` with a stated reason each.
- **Notification area (2026-09-18).** Location-targeted pushes read `push_target_areas` (fresh presence, else the coarse ~0.7 mi `notification_areas` row, 48h TTL) -- never raw presence alone. The row is written only via `set_my_notification_area` from the central location provider's fresh fixes (`services/notificationArea.js`, throttled), is not presence, and is read by nothing user-facing. Any new location-targeted push trigger must join `push_target_areas`.
- **Animation consistency (Item 137, audit 2026-09-18).** All motion goes through `src/motion/`:
  durations are `MOTION_BUDGET`/`SEQUENCES`/`AMBIENT` tokens (no literal `duration: N`), native modals use
  `modalAnimation()`, list refresh uses `PullToRefresh`, layout changes use `animateLayout()` (small tier,
  snaps under Reduce Motion), pure confirmations use `showSuccessToast` not `Alert`. Guarded by
  `src/motion/consistencyAudit.test.js`. Left deliberately: chat "load older" footer spinners (footer
  control), StoryViewer's functional progress timer, PhotoLightbox/swipe-card physics springs.
- **Product personality (Item 136, guiding thesis, logged 2026-09-18): Nearby is quietly working for
  you -- never "here's a database."** The interface should say, in effect: *tell us what you want, we'll
  figure out the rest.* Use as a tiebreaker for copy, motion and flow: prefer intent-first entry
  points over browse-and-filter, narrate real work calmly (Item 135), turn empty results into an
  invitation (Item 134), lead with a reason/outcome instead of a raw list or count, and keep motion
  restrained (Items 122/126/131). Avoid database voice ("0 results," "no data," raw field labels).
  Companion to the intent-engine north star (Item 112); no build item of its own -- apply it to
  whatever is being touched. Aesthetic target: modern + polished
  + alive + restrained — a premium social product, not a children's app; no confetti-everywhere.
- **Motion intensity varies by context (Item 122, locked 2026-09-18).** Occasion-creation moments
  (a plan being born, a birthday/anniversary pick, a community going live) can stay fully
  playful/celebratory. A real business TRANSACTION confirming (an offer accepted, a reservation
  locking in) must feel fast + trustworthy + professional instead — reassuring, not festive.
  `SuccessAnimation`'s `tone` prop (`"celebratory"` default vs. `"business"`) is the mechanism:
  business tone skips the ✨ discovery beat, settles in roughly half the time, and drops the
  springy scale-pop for a plain settle. Same content/meaning either way — only the intensity
  changes with context. Apply this same judgment to any future animated business-transaction
  moment (a payment confirming, an offer being made), not just the two call sites fixed here.
- **Anticipation treatments are scoped to real countdowns, not added to every date (Item 123,
  locked 2026-09-18).** Use `AnticipationText`/`anticipationTier()` (`src/motion/`,
  `src/utils/anticipationTier.js`) only where a real `days_until`-shaped countdown already exists
  and the date itself is genuinely the point (an occasion reminder) — never retrofit a countdown
  onto a surface built to show an exact date (a reminder list, a plan's own date line). Discrete
  tiers derived from a real day count, not a fabricated progress percentage.
- **Calendar = when, Nearby = what + who + where + how (Item 76, locked 2026-09-13; narrow
  export exception added by Item 121, 2026-09-18).** Nearby may read device calendar context
  (Item 75) to inform suggestions, plans, occasions, and Surprise Me, but must never become a
  calendar-management surface itself -- no new "Calendar" screen/tab, no general event
  creation/editing, no calendar-app-shaped view, no browsing/CRUD of existing events. The one
  disclosed exception: a single "Add to Calendar" export of one already-real, already-confirmed
  Nearby commitment (e.g. a confirmed reservation) via `expo-calendar`'s
  `createEventInCalendarAsync` -- the native OS compose UI, where the user themselves reviews and
  taps Save, never a silent background write via `createEventAsync`. This is a one-way EXPORT of a
  single fact, not management; any future calendar-adjacent work should still read and suggest,
  never manage or silently write.
- **No dead ends (Item 56, locked 2026-09-12).** No major surface's empty state may be
  unactionable "nothing here" copy alone — it must offer a concrete, tappable next step to a real
  existing destination (create/adjust-filters/invite/explore-elsewhere, whichever genuinely fits),
  never a fabricated one. Applies to any new empty state a future change introduces, not just the
  surfaces already audited under Items 25/26/56.
- **Coral (`colors.primary`) = action, not decoration.** Tappable-and-advances-the-user → coral.
  Informational → must not visually impersonate a button. Destructive → `colors.danger`, never
  coral. Progress/data-visualization (a fill bar, an achievement indicator) → coral is fine when
  clearly non-interactive. Secondary actions (Cancel, dismiss) → neutral/outlined; coral is
  reserved for a surface's *primary* action. This visual system is frozen — no further
  consistency sweeps expected unless new work introduces a genuinely new pattern.
- **No stranger discovery via intent, ever — hard privacy rule.** Any "find things for you"
  resolver-shaped feature (Home's intent box, Business Fulfillment matching, group-intent
  signals, etc.) may only ever surface real supply (gatherings/communities/businesses) or people
  the caller is already connected to (accepted friend or match) — never proximity/interest-based
  surfacing of an unconnected stranger. Businesses are deliberately exempt (they're discoverable
  supply by design, not a privacy concern the way a person is).
- **AI never infers or assigns a specific date/time from free text.** A user always picks
  date/time through deterministic UI (preset buttons + a picker). AI may suggest title/category/
  location/description for confirmation, never silently commit a date/time guess.
- **AI suggests, never silently commits.** Every AI-derived value anywhere in the app is shown
  back for the user's own explicit confirmation before it's saved — this holds for every
  AI-classification feature in this codebase, no exceptions.
- **Each actor only ever reports its own side's state.** A business says "I accept this Request"
  (→ becomes an Offer); a consumer says "I accept the Offer" (→ becomes a Commitment); Nearby's
  own SECURITY DEFINER RPCs compute the combined/derived state. No client ever directly flips
  another party's state.
- **Decline reasons feed a real, owner-visible insight surface — never automated re-weighting of
  the matching engine.** A business owner sees their own decline pattern and can manually tighten
  their own settings in response; nothing auto-adjusts matching behavior from decline history
  without its own separate, explicit authorization.
- **"Don't navigate for information, navigate for tasks"** (the Progressive Depth doctrine,
  locked Sep 15 2026 as a standing rule for all future UI work): a screen change should only ever
  happen when the user's actual task changes, or real information depth genuinely requires it —
  never merely because a filter, category, or already-visible data changed.
- **Feature-freeze convention**: don't start a new product surface or architectural change
  without a direct, explicit user request. This does not block bug fixes, security fixes, or
  stabilization work — those are always in scope. A direct request is always sufficient to
  proceed on something bigger; this has been explicitly invoked and overridden dozens of times
  since it was first declared (2026-08-15) and is really just describing normal operating mode.
- **Real external accounts / real money (Stripe, a real reservation/transportation provider,
  etc.) always need the user present for that decision** — never set up or connected
  autonomously, even if the schema/UI scaffolding around the seam is otherwise safe to build
  ahead of time.
- **Migration/verification discipline**: one migration file per schema change (never a
  duplicate hand-patch baked into a squashed baseline file in the same change — that exact
  mistake once broke this repo's own "rebuildable from an empty database" guarantee). Verify a
  schema change live against production with real disposable test data before considering it
  done; a full from-scratch Docker replay (`supabase/postgres:15.1.0.147`, drop/recreate an empty
  `public` schema, patch the two known image-version gaps — `auth.users.phone`,
  `storage.buckets.public` — onto the test container only, run the full `supabase/migrations/`
  folder in filename order via `psql -v ON_ERROR_STOP=1`) is the gold-standard extra proof this
  repo has historically done, but isn't mandatory for every small change — disclose plainly
  whether it was done, don't silently skip and claim parity.
- **`CREATE OR REPLACE FUNCTION` creates a second overload instead of replacing the original
  whenever the parameter list changes at all — even with an unchanged return type**, not only
  the already-documented RETURNS TABLE column-list case. Adding a new trailing default
  parameter to an existing function (discovered 2026-09-06 adding `update_business_profile`'s
  `subcategory_param`) leaves the old signature live and independently callable side by side
  with the new one, silently defeating the change for any caller still resolving to the old
  overload. Always re-check `pg_get_function_identity_arguments` for the function name right
  after any such migration; if more than one row comes back, `drop function` the old exact
  signature explicitly, and add that same explicit drop into the migration file itself before
  its `create or replace` so a from-scratch replay lands in the same single-overload state.
- **A new Postgres function defaults to PUBLIC execute access** — always explicitly
  `revoke ... from public, anon` unless it's genuinely meant to be public. Rate-limit/counter
  triggers use `SELECT ... FOR UPDATE` to avoid race conditions. Privileged `profiles` columns
  (`is_premium`, `managed_partner_id`, daily-counter columns, etc.) are guarded by a
  `prevent_self_premium_edit()`-style trigger; a legitimate server-side write to one of these
  must `perform set_config('app.trusted_update', 'true', true)` first.
- **Git workflow for this repo**: commit and push after each individual phase/increment as it
  lands, not batched at the end — this is this project's own long-standing, explicitly
  pre-authorized convention (not something to re-confirm each time), specifically so a
  mid-session restart never loses more than one increment's worth of work.
- **Migration filename ordering matters.** Migrations replay in filename lexical order,
  independent of when they were actually written — a new migration that depends on an earlier
  one must sort *after* it by filename, or a from-scratch replay will fail even though production
  (already migrated in real chronological order) looks fine. This has bitten this repo more than
  once; double-check filename ordering against real dependencies before naming a new migration.
- **Business web export (`docs/business/`).** Path A Expo web export of the business dashboard,
  served at `/Nearby/business/` via GitHub Pages. Regenerate (`NEARBY_WEB_EXPORT_BASE_URL=/Nearby/business
  npx expo export -p web`, copy `dist/*` over `docs/business/`) and recommit whenever a
  business-facing screen changes — no CI builds it. Never put `experiments.baseUrl` back into
  `app.json` (it's global and breaks native iOS builds); it's injected by `app.config.js` only when
  that env var is set. Web-only surface: `App.web.js`, `BusinessWebNavigator.js`,
  `BusinessWebHomeScreen.js`, `PlatformDateTimeInput.js`; not verified in a real browser.

## Reference

- `CLAUDE_HISTORY.md` — the complete historical build log (pre-2026-09-17), unedited, reverse-
  chronological. Grep by date or feature name.
- `PRODUCT_AUDIT/` — standalone audit documents from past sessions, mostly historical snapshots.
  `PRODUCTION_ARCHITECTURE_2026-08-15.md` (system-wide architecture reference) and
  `SIGNAL_CONTRACT.md` (per-signal collection/matching/ranking contract) are the two most likely
  to still be useful as a reference rather than pure history.
- No automated test framework beyond Jest unit tests on pure functions
  (`jest.config.js`/`jest.babel.config.js`) — no simulator/device testing has ever been available
  in any session on this project.
