# Feature Matrix — Nearby

*Basis: `SCREEN_INVENTORY.md`, `DATABASE_AND_DATA_MODEL.md`, `NAVIGATION_AND_IA.md`, and direct
greps of `src/services/`. **Refreshed 2026-08-09** — every row reflects the current repo plus,
where noted, live production re-verification performed during this refresh (not just static code
reading). "Complete?" reflects what was directly observed working end-to-end — it does not
certify production behavior that couldn't be checked from this repo alone (e.g. a live Anthropic
call). Where evidence was thin, the row says so explicitly.*

| Feature | Exists? | Complete? | Where implemented | Notes |
|---|---|---|---|---|
| Authentication | Yes | Yes | `LoginScreen.js`, Supabase Auth (phone OTP) | Unchanged. Still includes the hardcoded App-Review reviewer-phone bypass — see `IMPLEMENTATION_NOTES.md`. |
| Onboarding | Yes | Partial | `Onboarding`→`OnboardingQuestions`→`OnboardingLocation`→`Login`→`CompleteProfile`→`OnboardingRecommendations` | **Improved**: the recommendation-card navigation bug is FIXED (cards now deep-link to the specific tapped gathering). The location-preference answer is still collected but never used downstream — unchanged. |
| Profiles | Yes | Yes | `ProfileScreen.js`, `ViewProfileScreen.js`, `profiles` table | Unchanged. |
| Discover (unified hub) | Yes | Yes | `DiscoverHubScreen.js` | Unchanged. |
| Search | Yes | Partial | client-side filter in `DiscoverHubScreen.js`; live Google Places query for Places | Unchanged — still not a real backend index, confirmed **STILL PRESENT** this refresh. |
| Gatherings | Yes | Yes | `GatheringsScreen.js`, `GatheringDetailScreen.js`, `GatheringHubScreen.js`, `gatherings`/`gathering_interest` tables | **Improved**: `join_gathering()`'s invite-only enforcement is now real, server-side, and live-verified (both directions — uninvited caller rejected, invited-accepted caller succeeds), closing a previously-accepted UI-only-gating risk. An honest "almost full" capacity nudge was also added. |
| Communities | Yes | Yes | `CommunitiesScreen.js`, `CommunityDetailScreen.js`, `community_members` | **Improved**: a community can now be seeded directly from a past gathering's real, friended attendees ("Start a Community from This Gathering"), and community-scoped business perks are now surfaced to members (previously invisible). The historical `community_members`/`communities` RLS mutual-recursion bug is confirmed **live-fixed** this refresh (see `DATABASE_AND_DATA_MODEL.md`). |
| Create (unified flow) | Yes | Yes | `CreateHubScreen.js`, `CreateGatheringScreen.js`, `create-assistant` Edge Function | Unchanged. |
| Invitations | Yes | Yes — upgraded from Partial | `social_invites` table, `InviteFriendsModal.js`, `Inbox` Invites tab | **Improved**: a real "Invite someone not on Nearby yet" share-link path now exists for a specific gathering (closing the last audit's #11 P1 finding), and cold-start push-tap delivery for an invite notification is now reliable (see `AUDIT_CHANGELOG.md`). Still a genuinely separate mechanism from the app-referral "invite" screen — that overlap is unchanged. |
| Connections (friends) | Yes | Yes | `FriendsScreen.js`, `friendships` table, `friend_circles` | Unchanged. |
| Messaging (1:1) | Yes | Yes — upgraded from Partial | `ChatScreen.js`, `messages` table | **Both prior bugs FIXED**: the production debug overlay is gone, and a failed send now restores the composer text with a visible error instead of silently dropping the message. |
| Group chats | Yes | Yes — upgraded from Partial | `GatheringChatScreen.js`, `CommunityChatScreen.js` | Silent-send-failure pattern FIXED, same shared fix as 1:1 chat. |
| Activity | Yes | Yes | `ActivityScreen.js` (registered as the `Notices` route) | **Improved (hygiene)**: the superseded `NoticesScreen.js` dead-code file is now deleted outright, not just orphaned. |
| Notifications | Yes | Yes — upgraded from Partial | `services/notifications.js`, `send-push` Edge Function | **Improved**: a real, previously-undocumented gap is closed — any push tap (match/message/wave/friend_request/gathering_invite/reminder/reward-nudge/etc.) that launched the app from a fully-closed state was silently dropped; now stashed and replayed once the authenticated stack mounts. Verified by direct code reading only — no on-device cold-start test is possible in this sandbox. Whether every server-side trigger that's supposed to send a push actually fires in production was not independently re-verified this pass beyond what `CLAUDE.md`'s own account already covers for `send-momentum-reward-nudges`/`send-gathering-reminders` (confirmed live-active `cron.job` rows). |
| Business profiles | Yes | Yes | `BusinessProfileScreen.js`, `brand_partners` table | Unchanged. |
| Business mode | Yes | Yes (as a concept), still thin as a "mode" | `profiles.managed_partner_id` conditional UI | Unchanged — same duplicated-gating-logic caveat as the last audit. |
| Business dashboard | Yes | Yes — upgraded from Partial | `BusinessDashboardScreen.js` | **The last audit's real finding is fixed**: the dashboard's own "isn't available yet" profile-editing message is replaced with a real, ownership-checked self-edit RPC + modal. Also gained persistent per-customer CRM notes/tags and an AI assistant entry point (see rows below). |
| Business partnerships | Yes | Yes | `RequestBusinessPartnerScreen.js`, `BusinessPartnerApplyScreen.js`, `business_partnership_requests`/`business_partner_requests` | Unchanged — the naming-overlap risk between the two flows is unchanged. |
| Business CRM (per-customer) | Yes | Yes — **new row this refresh** | `business_customer_notes` table, `BusinessDashboardScreen.js`'s "Most Engaged" drill-in | New since the last audit: persistent notes/tags per customer, owner-only RLS + 2 SECURITY DEFINER RPCs, live-confirmed ownership-scoped (non-owner correctly rejected and correctly sees zero rows). Closes part of the last audit's #12/CRM gap. |
| Business AI Assistant | Yes | Partial — **new row this refresh** | `supabase/functions/business-ai-assistant`, `BusinessAIAssistantScreen.js` | New since the last audit. Ownership gate, rate limit, and the dual-client `auth.uid()` pattern are all confirmed live-correct. The actual Anthropic call path itself was not exercised end-to-end (same disclosed gap as `ai-concierge`). |
| Perks/rewards | Yes | Yes | `BrandOffersScreen.js`, `RewardsScreen.js`, `brand_offers`, `offer_redemptions` | **Improved**: `RewardsScreen` now has a real outbound CTA (was a dead end). |
| Redemption | Yes | Yes — upgraded from Partial | `redeemOffer()`/`confirmOfferRedemption()` in `services/brandOffers.js` | **The last audit's real gap is closed**: a real 6-digit proof-of-redemption confirmation-code flow now exists (claim → code shown to user → business enters code → `confirmed_at` set) — closing the "how does a business confirm a redemption actually happened" UNCLEAR from the last audit. |
| Billing (business side) | Yes | Partial | `partner_contracts`, `business_invoices`, monthly `pg_cron` invoice generator | **Unchanged, confirmed STILL PRESENT**: only *confirmed* redemptions now count toward the billing math (a real improvement), but no payment processor exists at all — invoices still sit in `draft` forever. |
| Payments (consumer subscription) | Yes | Yes | `PaywallScreen.js`, `BillingScreen.js`, RevenueCat | Unchanged. |
| Location | Yes | Yes (privacy-preserving by design) | `expo-location`, `presence_reports`/`sightings` tables | **Independently re-confirmed live this refresh**: `profiles` has zero lat/lng-shaped columns among its real 66 columns. |
| Maps | Yes | Yes | `GatheringsMapView.js`, `react-native-maps` | Unchanged. |
| Safety/reporting | Yes | Yes | `ReportBlockModal.js`, `reports` table, `date_checkins`, `EmergencyContactsScreen.js` | Unchanged. |
| Blocking | Yes | Yes — **confirmed live, not just reported** | `blocks` table, `is_blocked()` function | The last audit's biggest open question is resolved: `is_blocked()`'s `SECURITY DEFINER` fix was independently re-tested live this refresh, both directions, using a real disposable block row — the blocked party (the historically-broken case) correctly gets `true` back, an unrelated third party correctly gets `false` (anti-probing guard intact). |
| Privacy | Yes | Mostly, with one explicit trade-off | RLS across most tables; `gatherings`/`communities` RLS is deliberately wide-open (app-layer gating only) | Unchanged design posture. A previously-open, previously-unresolved question — whether a host could self-set `hosting_partner_id` on their own gathering with no business consent — is now confirmed live-**secure** (a dedicated trigger blocks it) — see `DATABASE_AND_DATA_MODEL.md`. |
| AI features | Yes | Partial | `create-assistant`, `ai-concierge`, `business-ai-assistant` Edge Functions, several `generate-*` functions | **New**: `business-ai-assistant` added. All are confirmed correctly deployed (`verify_jwt: true`, real 401 on unauthenticated calls) and structurally sound — but end-to-end success of the actual model call still was not exercised in any documented session, same unresolved gap as the last audit. |
| Premium features | Yes | Yes | `PaywallScreen.js`, `noticeLimits.js`/`waveLimit`/`browseLimits.js`/`gatheringLimits.js`/`voiceNoteLimits.js`, `ai-concierge` | **Confirmed live this refresh**: the server-side premium/limit checks these depend on (`bonus_notices`/`is_premium` guarded-column protection, `check_and_increment_ai_use` grants) were live self-escalation-tested and confirmed to hold. |
| Analytics (product/user-facing) | Yes | Yes | `InsightsScreen.js`, `MomentumScreen.js`, `RewardsScreen.js` | **Improved**: all three now have a real outbound CTA — the last audit's specific "dead end" complaint for this row is resolved. |
| Analytics (business-facing) | Yes | Yes — **confirmed live, not just reported** | `BusinessDashboardScreen.js`'s stats/growth/insights RPCs | The last audit's biggest business-side open question is resolved: all 5 original RPCs plus the 3 new-this-session ones were live-verified to carry the ownership check and to be correctly grant-restricted (`anon=false`, `authenticated=true`) — no PII leak confirmed for any of the 8. |
| Analytics (operator/telemetry) | Yes | Yes | PostHog (`App.js`), Sentry (`App.js`) | Unchanged. |
